# استخدم الصورة الرسمية مباشرة
FROM n8nio/n8n:latest

# Render يحتاج لتحديد المستخدم كـ root أحياناً لتجاوز صلاحيات المسارات
USER root

# إعداد المنفذ كما طلبنا سابقاً
ENV N8N_PORT=10000
EXPOSE 10000

# التعديل الجوهري هنا: استدعاء n8n عبر مساره الكامل أو تركه للصورة الأصلية
ENTRYPOINT ["n8n"]
